package com.example.myapplication.object;

/*
public class Player extends GameObject {

    private double radius;
    private Paint paint;

    private Sprite sprite;


    public Player(Context context, double positionX, double positionY, double radius, Sprite sprite){

        super(positionX, positionY);
        //this.positionX = positionX;
        //this.positionY = positionY;
        this.radius = radius;

        paint = new Paint();
        int color = ContextCompat.getColor(context, R.color.player);
        paint.setColor(color);

        this.sprite = sprite;
    }

    public void draw(Canvas canvas, GameDisplay gameDisplay) {

        /*
        sprite.draw(
                canvas,
                //set in right hand corner
                //(int)gameDisplay.gameToDisplayCoordinatesX(getPositionX()) - sprite.getWidth()/2,
                (int)gameDisplay.gameToDisplayCoordinatesX(getPositionX()) + 850,
                (int)(gameDisplay.gameToDisplayCoordinatesY(getPositionY())) - 450
                );
        //canvas.drawCircle((float)positionX, (float) positionY, (float)radius, paint);




    }



    public void update() {

    }

    public void setPosition(double positionX, double positionY) {
        this.positionX = positionX;
        this.positionY = positionY;
        //positionX = positionX;
        //positionY = positionY;
    }
}
*/
